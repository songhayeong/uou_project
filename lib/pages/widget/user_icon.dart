import 'package:flutter/material.dart';

class UserIcon extends StatelessWidget {
  const UserIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
    // 얘가 필요할려나 ? .... 굳이 필요없지 싶은데 IMHO에 의하면 location page에서만
    // 쓰이기 때문에 굳이 안써도 될듯하다.
  }
}
